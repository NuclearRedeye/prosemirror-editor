{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Client",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:5000/",
      "webRoot": "${workspaceRoot}/dist",
      "preLaunchTask": "npm: build"
    }
  ]
}
